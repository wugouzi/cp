impl Solution {
    pub fn rearrange_barcodes(b: Vec<i32>) -> Vec<i32> {
        let mut ans = Ve
        ans
    }
}

pub struct Solution;

#[cfg(test)]
mod tests {
    extern crate test;
    use std::env;
    use super::*;

    #[test]
    fn test() {
        env::set_var("RUST_BACKTRACE", "1");
        assert_eq!(Solution::rearrange_barcodes(), );
    }

}
