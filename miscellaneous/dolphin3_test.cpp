#include <iostream>
#include <vector>
#include <queue>
#include <cstring>
#include <climits>
#include <cmath>
#include <algorithm>
#include <stack>
#include <unordered_map>
#include <fstream>
using namespace std;

typedef long long ll;
typedef pair<ll,ll> pp;
#define mp(t1,t2) (make_pair((t1),(t2)))

void generate(int n) {
  srand(n);
  vector<int> nums(n), nums2;
  for (int i = 0; i < n; i++) {
    nums[i] = rand() % n;
  }
  nums2 = nums;
  sort(nums.begin(), )

  fstream f("")
}

int main(void) {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);


  return 0;
}
