#include "leetcode.h"

class RangeModule {
    public:
        class Node {
            int ls,rs,sum,add;
        };

        int N = 1e9+10, M=500010, cnt;
        Node *tr = new Node[M];
        void update(int u, int lc, int rc, int l, int r, int v) {
            int len = rc - lr + 1;
            if (l <= lc && )
        }
        RangeModule() {

        }

        void addRange(int left, int right) {

        }

        bool queryRange(int left, int right) {

        }

        void removeRange(int left, int right) {

        }
};
